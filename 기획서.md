# 회원가입 및 로그인 서비스 - Frontend 기획서

## 1. 개요
사용자가 이메일/비밀번호 기반으로 회원가입, 로그인, 로그아웃을 수행할 수 있는 프론트엔드 애플리케이션을 개발한다. Backend에서 제공하는 REST API와 연동하여 인증 상태를 관리한다.

## 2. 목표
- 신규 사용자가 간편하게 계정을 생성할 수 있도록 한다.
- 기존 사용자가 안전하게 로그인/로그아웃할 수 있도록 한다.
- 인증 토큰을 안전하게 저장하고, 로그인 상태를 전역에서 관리한다.

## 3. 주요 화면
| 화면 | 설명 |
|---|---|
| 회원가입 페이지 | 이메일, 비밀번호, 비밀번호 확인, 닉네임 입력 후 가입 |
| 로그인 페이지 | 이메일, 비밀번호 입력 후 로그인 |
| 마이페이지(선택) | 로그인된 사용자 정보 표시, 로그아웃 버튼 |
| 비밀번호 재설정(선택) | 이메일로 재설정 링크 발송 |

## 4. 사용자 플로우
### 회원가입
1. 사용자가 회원가입 폼 입력
2. 클라이언트 측 유효성 검사(이메일 형식, 비밀번호 규칙, 비밀번호 일치 여부)
3. `POST /api/auth/signup` 호출
4. 성공 시 로그인 페이지로 이동 (또는 자동 로그인 처리)
5. 실패 시 에러 메시지 표시 (예: 이메일 중복)

### 로그인
1. 사용자가 이메일/비밀번호 입력
2. `POST /api/auth/login` 호출
3. 성공 시 Access Token(및 Refresh Token) 저장, 메인 페이지로 이동
4. 실패 시 에러 메시지 표시 (예: 비밀번호 불일치, 존재하지 않는 계정)

### 로그아웃
1. 로그아웃 버튼 클릭
2. 저장된 토큰 삭제 및 `POST /api/auth/logout` 호출
3. 로그인 페이지로 이동

## 5. 입력 유효성 검사 규칙
- 이메일: 형식 검증 (`example@domain.com`)
- 비밀번호: 최소 8자, 영문/숫자/특수문자 조합 권장
- 비밀번호 확인: 비밀번호와 일치 여부 확인
- 닉네임: 2~12자, 특수문자 제한

## 6. 인증 상태 관리
- Access Token은 메모리 또는 상태관리 스토어에 저장
- Refresh Token은 HttpOnly Cookie 사용 권장 (XSS 방지)
- 전역 인증 상태(로그인 여부, 사용자 정보)는 Context API 또는 상태관리 라이브러리(Redux/Zustand 등)로 관리
- Access Token 만료 시 Refresh Token으로 재발급 처리 (Axios Interceptor 등 활용)

## 7. API 연동 목록 (Backend와 협의)
| Method | Endpoint | 설명 |
|---|---|---|
| POST | /api/auth/signup | 회원가입 |
| POST | /api/auth/login | 로그인 |
| POST | /api/auth/logout | 로그아웃 |
| POST | /api/auth/refresh | 토큰 재발급 |
| GET | /api/users/me | 내 정보 조회 |

## 8. 에러 처리
- 입력값 오류: 폼 하단에 인라인 메시지 표시
- 서버 에러(4xx/5xx): 공통 에러 처리 로직(Toast 또는 Alert)으로 안내
- 네트워크 오류: 재시도 안내 메시지 표시

## 9. 기술 스택 (제안)
- Framework: React (또는 Next.js)
- 상태관리: Zustand / Redux Toolkit
- HTTP 통신: Axios
- 폼 관리 및 유효성 검사: React Hook Form + Zod/Yup
- 스타일링: Tailwind CSS

## 10. 향후 확장 고려사항
- 소셜 로그인 (구글, 카카오 등)
- 이메일 인증
- 2단계 인증(2FA)
- 자동 로그인(Remember Me)
